# Raw data

Place the original fuel-consumption and vehicle-specification CSV files here if you want to rerun the full notebook from raw inputs.

The current repository update includes the processed outputs in `data/processed/`.
